/**
 * Stub for optional packages that wagmi's connector barrel imports but never executes
 * (the @x402/* family, React Native storage). Used by the Turbopack resolveAlias in
 * next.config.ts; the webpack build ignores those specifiers instead.
 */
export default {}
